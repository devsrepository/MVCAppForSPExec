
@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<div class="container">

    <div class="row">
        <div class="col-lg-12 d-flex align-items-stretch">
            <div class="table-responsive">
                <table id="tblAppointment" class="table table-bordered table-dark table-hover">
                    <thead class="table-responsive">
                        <tr class="table-responsive">
                            <th class="table-primary">RegId</th>
                            <th class="table-secondary">Name</th>
                            <th class="table-light">MobileNo</th>
                            <th class="table-info">CityName</th>
                            <th class="table-warning">StateName</th>
                        </tr>


                    </thead>
                    <tbody>
                        @*this should be declared otherwise it will be not shown.*@
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
